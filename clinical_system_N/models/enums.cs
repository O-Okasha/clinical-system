enum CrudType
{
    insert,
    update,
    delete
};

enum ClinicType
{
    physio,
    ortho
};

enum Gender
{
    male,
    female,
}
enum MaritialStatus
{
    Married,
    Divorced,
    Widowed,
    Single
}